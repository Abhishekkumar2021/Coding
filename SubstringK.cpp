#include<bits/stdc++.h>
using namespace std;

int maxLength(string &s, int k){
    int len = 0;
    int i = 0, j = 0, n = s.length(), x = 0, cnt = 0;
    vector<int> freq(256, 0);
    // while(j<n){
    //     freq[s[i]]++;
    //     if(freq[s[i]] == 1) x++;
    //     if(freq[s[i]] == k) cnt++;

    //     // If we have found a substring
    //     if(cnt == x){

    //     }
    //     j++;
    // }
    // builda a new array using for_each and lambda
}