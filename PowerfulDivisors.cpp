#include<bits/stdc++.h>
using namespace std;

vector<int> powerfulDivisors(vector<int> &A) {
    int maxi = *max_element(A.begin(), A.end());
    vector<int> count(maxi+1, 0);
    // count[i] stores the number of divisors of i

    
}